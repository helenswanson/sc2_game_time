<!DOCTYPE html>
<html lang="en">
  <head>
    <title>NFL Team: <%= @team_title %></title>
  </head>

  <body>
    <h1>
      NFL Team: <%= @team_title %>
    </h1>
    <p>
      <% @sorted_scorecard.each do |team_info| %>
        <% if @team_title == team_info[0] %>
          <%="#{team_info[0]}"%></a> - wins: <%="#{team_info[1][:wins]}"%> - losses: <%="#{team_info[1][:losses]}"%> - ties: <%="#{team_info[1][:ties]}"%>
        <% end %>
      <% end %>
      <br/><br/><br/>

      <% @game_matchups.each do |team_info| %>
        <% if @team_title == game_info["home_team"] or @team_title == game_info["away_team"] %>
          Home team: <%="#{team_info["home_team"]}" %>
          Away team: <%="#{team_info["away_team"]}" %>
          Home score: <%="#{team_info["home_score"]}" %>
          Away score: <%="#{team_info["away_score"]}" %>
          <br/><br/>
        <% end %>
      <% end %>

      <br/><br/><br/>
      <a href="http://localhost:4567/">Back</a>
    </p>
  </body>
</html>
